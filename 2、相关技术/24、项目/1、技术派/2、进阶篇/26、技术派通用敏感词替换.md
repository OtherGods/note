
数据脱敏对于国内的互联网企业来说，属于标配，如果不想某天突然被查水表的话（题外话：技术派的水表都被抄了😂）

接下来给大家介绍一下技术派中是怎么实现数据脱敏的

# 1、敏感词校验

现在的敏感词校验算法相对成熟，github上也可以搜到很多相关的实现，接下来简单给大家介绍一下敏感词校验的算法原理

## 1.1、DFA算法

> 偷个懒，直接从网上copy一份相关的介绍：[游戏中敏感词的过滤之DFA算法 - 知乎](https://zhuanlan.zhihu.com/p/214347618)

DFA 算法是通过提前构造出一个 树状查找结构(实际上应该说是一个 森林)，之后根据输入在该树状结构中就可以进行非常高效的查找。

设我们有一个敏感词库，词酷中的词汇为：
我爱你
我爱他
我爱她
我爱你呀
我爱他呀
我爱她呀
我爱她啊

那么就可以构造出这样的树状结构：
![image.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202401071718766.png)

假设需要校验的字符串为：一灰我爱你呀哈哈哈

我们遍历输入的字符串 str，并设指针 i 指向树状结构的根节点，即最左边的空白节点：
```java
str[0] = ‘一’ 时，此时 tree[i] 没有指向值为 ‘一’ 的节点，所以不满足匹配条件，继续往下遍历
str[1] = ‘灰’，同样不满足匹配条件，继续遍历
str[2] = ‘我’，此时 tree[i] 有一条路径连接着 ‘我’ 这个节点，满足匹配条件，i 指向 ‘我’ 这个节点，然后继续遍历
str[3] = ‘爱’，此时 tree[i] 有一条路径连着 ‘爱’ 这个节点，满足匹配条件，i 指向 ‘爱’，继续遍历
str[4] = ‘你’，同样有路径，i 指向 ‘你’，继续遍历
str[5] = ‘呀’，同样有路径，i 指向 ‘呀’
此时，我们的指针 i 已经指向了树状结构的末尾，即此时已经完成了一次敏感词判断。我们可以用变量来记录下这次敏感词匹配开始时玩家输入字符串的下标，和匹配结束时的下标，然后再遍历一次将字符替换为 * 即可。
结束一次匹配后，我们把指针 i 重新指向树状结构的根节点处。
此时我们玩家输入的字符串还没有遍历到头，所以继续遍历：
str[6] = ‘哈’，不满足匹配条件，继续遍历
str[7] = ‘哈’ …
str[8] = ‘哈’ …
```

可以看出我们遍历了一次玩家输入的字符串，就找到了其中的敏感词汇。

而在这一段标题的下面，我说 DFA 算法一开始构造的结构实际上算是一种森林，因为对于一个更完整的敏感词库而言，其构造出来的结构是这样的：
![image.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202401071724847.png)

如果不看该结构的根节点，即那个空白节点，那么就可以看作是由一个个树结构组成的森林。

## 1.2、敏感词服务类

直接从github中找了一个star最多的库，有兴趣的小伙伴也可以瞅一下

> [https://github.com/houbb/sensitive-word](https://github.com/houbb/sensitive-word)

技术派中的使用姿势如下:

新增一个敏感词配置类，处理自定义的敏感词、与白名单
```java
@Data
@Component
@ConfigurationProperties(prefix = "paicoding.sensitive")
public class SensitiveProperty {
    /**
     * true 表示开启敏感词校验
     */
    private Boolean enable;
    
    /**
     * 自定义的敏感词
     */
    private List<String> deny;

    /**
     * 自定义的非敏感词
     */
    private List<String> allow;
}
```

结合技术派中实现的配置动态变更刷新机制([28、技术派自定义配置注入&动态刷新](2、相关技术/24、项目/1、技术派/2、进阶篇/28、技术派自定义配置注入&动态刷新.md))，我们封装了一下支持敏感词动态变更的服务类
```java
@Service
public class SensitiveService {
    private SensitiveProperty sensitiveConfig;

    private volatile SensitiveWordBs sensitiveWordBs;

    public SensitiveService(DynamicConfigContainer dynamicConfigContainer, SensitiveProperty sensitiveConfig) {
        this.sensitiveConfig = sensitiveConfig;
        // 注册配置之后，回调 refresh 方法，实现敏感词库变更的动态刷新
        dynamicConfigContainer.registerRefreshCallback(sensitiveConfig, this::refresh);
    }

    @PostConstruct
    public void refresh() {
        IWordDeny deny = () -> {
            List<String> sub = WordDenySystem.getInstance().deny();
            sub.addAll(sensitiveConfig.getDeny());
            return sub;
        };

        IWordAllow allow = () -> {
            List<String> sub = WordAllowSystem.getInstance().allow();
            sub.addAll(sensitiveConfig.getAllow());
            return sub;
        };
        sensitiveWordBs = SensitiveWordBs.newInstance()
                .wordDeny(deny)
                .wordAllow(allow)
                .init();
        log.info("敏感词初始化完成!");
    }

    /**
     * 判断是否包含敏感词
     *
     * @param txt
     * @return
     */
    public boolean contains(String txt) {
        if (BooleanUtils.isTrue(sensitiveConfig.getEnable())) {
            return sensitiveWordBs.contains(txt);
        }
        return false;
    }


    /**
     * 敏感词替换
     *
     * @param txt
     * @return
     */
    public String replace(String txt) {
        if (BooleanUtils.isTrue(sensitiveConfig.getEnable())) {
            return sensitiveWordBs.replace(txt);
        }
        return txt;
    }

    /**
     * 查询文本中所有命中的敏感词
     *
     * @param txt 校验文本
     * @return 命中的敏感词
     */
    public List<String> findAll(String txt) {
        return sensitiveWordBs.findAll(txt);
    }
}
```

具体的使用就比较简单了，目前主要是两个部分使用了敏感词的校验

- 派聪明的提问
  ![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030014944.png)
- 评价的敏感词替换

这个是基于Mybatis的插件机制，直接针对查询出来的字段进行敏感词替换

# 2、自定义的数据库敏感词替换方案

接下来我们看一下实际生产项目中使用的数据脱敏机制，为了安全以及某些大家都知道的原因，db中有很多信息是不能存储明文的，比如身份证、银行卡等需要加密之后存储到数据库中，然后再读取时，解密返回对应的明文

那么上面这一套怎么实现呢？
- 直接编码实现，每次写的时候、读取的时候手动进行加解密 ？
- 实现一个通用的解决方案，比如在需要脱敏的字段上加一个标识，然后再实际写db/从db读取时，自动实现加解密

显然我们这里介绍的是第二个解决方案

## 2.1、基于mybatis拦截器的敏感词替换实现方案

整体的方案思路比较清晰：

1. 实现一个自定义注解，放在需要脱敏的**数据库实体对象**的成员上
2. 实现查询拦截器，再返回数据库内容到数据库实体对象上时，若判断成员上有对应注解，则赋值敏感词替换之后的内容

再具体的实现层，我们会增加一下缓存，减少每次都对实体对象的成员都进行判定，是否需要脱敏

所有相关代码可以再 com.github.paicoding.forum.core.senstive 下查看
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030016369.png)

下面针对几个关键的实现进行针对性说明

**拦截器实现：com.github.paicoding.forum.core.senstive.ibatis.SensitiveReadInterceptor**
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030016390.png)

核心就两步：

1. 根据返回结果，找到对应的实体类，然后找需要进行替换的成员
2. 执行具体的敏感词替换

**敏感词替换元数据信息：com.github.paicoding.forum.core.senstive.ibatis.SensitiveObjectMeta**
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030017364.png)

构建需要进行敏感词替换的元数据信息的实现相对而言就好理解一些了，无非就是利用反射，拿数据库实体对象的所有成员，然后判断是否有自定义注解SensitiveField，有则表示需要将这个信息记录下来，再后面就需要进行替换了

**敏感词替换: com.github.paicoding.forum.core.senstive.ibatis.SensitiveReadInterceptor#replaceSensitiveResults**

最后再看一下敏感词替换的实现：
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030017000.png)

最后我们再来看一下实际的效果（以实际生产的为例，敏感词替换为 `*` 号）
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030018617.png)

再来看一下实际的db中存储
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030018454.png)

(这个敏感词居然是码，这我也是没有想到)

那就顺带给大家再演示下，添加敏感词白名单之后，立即生效；再后台进行 全局配置 进行动态维护（这个只有管理员才能看到🐶,想体验的小伙伴建议自己本地进行测试验证）
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030019388.png)

然后再抽一下刚才的评论，如预期一致恢复正常
![1.png](https://raw.githubusercontent.com/OtherGods/MaterialImage/main/img/202410030019463.png)

# 3、小结

最后按照惯例小结一下，本文介绍的知识点非常实用，虽然一般也没有几个小伙伴有机会来实际操刀（因为这个一般是公司的基础部门进行支撑，而且也只有新开项目才有这个机会）

敲重点：

1. 敏感词校验算法：DFA
2. 一种基于mybatis拦截器的自定义数据库脱敏的技术方案

最后也希望可以通过这篇文章，给大家两个启发，我是你们的老朋友一灰灰，以下仅一家之言，不承担任何责任

1. 作为一个合格的研发，需要多思考能不能将一些直接代码实现的场景，抽象为通用的基础服务能力
2. 安全与合规非常重要，不要去搞别人，但是自己的系统也一定要有防止被别人搞的能力😂